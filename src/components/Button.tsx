const Button = () => {
  return (
    <button className="text-lg bg-red-500 text-white px-4 py-2 rounded hover:bg-red-800 focus:outline-none">
      Click Me
    </button>
  );
};

export default Button;
